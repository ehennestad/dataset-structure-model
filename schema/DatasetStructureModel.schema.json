{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DatasetStructureModel",
  "description": "A schema for describing the structure of scientific datasets, capturing their physical layout, semantic organization, and the mapping between the two. This schema answers: 'How is my data organized and where do I find entities and their metadata?'",
  "type": "object",
  "required": ["schemaVersion", "dataLocations", "preferences"],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": {
      "type": "string",
      "description": "Version of the DatasetStructureModel schema using semantic versioning",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "default": "1.0.0"
    },
    "entityTypes": {
      "type": "array",
      "description": "List of semantic entity types present in this dataset",
      "items": {
        "type": "object",
        "required": ["name"],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string",
            "description": "Name of the entity type (e.g., 'subject', 'session')"
          },
          "description": {
            "type": "string",
            "description": "Optional description of what this entity represents"
          },
          "color": {
            "type": "string",
            "description": "Optional color for UI visualization (e.g., '#FF0000')"
          },
          "isPrimary": {
            "type": "boolean",
            "description": "Whether this is a primary entity type in the model"
          },
          "identifierRef": {
            "type": "string",
            "description": "Key of the metadataDefinitions field that uniquely identifies an entity of this type across all data locations. Two entities of this type from different locations are considered the same real-world entity when their extracted values for this metadata field are equal (and their parent entities are also matched). If absent, matching falls back to comparing the raw folder or file name. IMPORTANT: Extraction rules for this metadata field across all data locations MUST produce identical values for the same real-world entity. If locations use different naming conventions, apply 'normalize' on the extraction rules to ensure values converge."
          },
          "identifierRefs": {
            "type": "array",
            "description": "Composite cross-location identity key. Use instead of identifierRef when identity requires multiple metadata fields (e.g., subject_id + session_number together). All referenced fields must be present and equal for two entities to be considered the same. IMPORTANT: The same normalization constraint as identifierRef applies to each referenced field.",
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    "entityRelationships": {
      "type": "array",
      "description": "Defines semantic relationships between entity types in this dataset, independent of physical storage layout",
      "items": {
        "$ref": "#/definitions/entityRelationship"
      }
    },
    "metadataDefinitions": {
      "type": "object",
      "description": "Global definitions of metadata fields that apply across all data locations. Each field must be uniquely named and associated with a specific entity type. All references to metadata must point to one of these global definitions.",
      "additionalProperties": {
        "type": "object",
        "required": ["name", "ofEntity", "dataType"],
        "properties": {
          "name": {
            "type": "string",
            "description": "Human-readable name of the metadata field"
          },
          "title": {
            "type": "string",
            "description": "User-facing display name (e.g., 'Subject ID')"
          },
          "description": {
            "type": "string",
            "description": "Description of what this metadata represents"
          },
          "dataType": {
            "type": "string",
            "enum": ["string", "number", "integer", "date", "time", "datetime", "boolean", "array", "object"],
            "default": "string"
          },
          "unit": {
            "type": "string",
            "description": "Unit of measurement for numeric metadata fields. Ideally UCUM-compatible (e.g., 'µm', 'Hz', 's', 'kg'). For dimensionless quantities, omit this field."
          },
          "defaultValue": {
            "type": ["string", "number", "boolean", "null"],
            "description": "Default value for this metadata field"
          },
          "ofEntity": {
            "type": "string",
            "description": "Name of the entity type this metadata field belongs to (must match a name in entityTypes)"
          },
          "validation": {
            "type": "object",
            "description": "Validation rules for this metadata field",
            "properties": {
              "pattern": {
                "type": "string",
                "description": "Regex pattern the value must match"
              },
              "minLength": {
                "type": "integer",
                "description": "Minimum length for string values"
              },
              "maxLength": {
                "type": "integer",
                "description": "Maximum length for string values"
              },
              "minimum": {
                "type": "number",
                "description": "Minimum value for numeric values"
              },
              "maximum": {
                "type": "number",
                "description": "Maximum value for numeric values"
              },
              "enum": {
                "type": "array",
                "description": "List of allowed values",
                "items": {}
              }
            }
          }
        }
      }
    },
    "dataLocations": {
      "type": "array",
      "description": "Array of data location definitions that represent different types of data with their own organizational structure",
      "items": {
        "$ref": "#/definitions/dataLocation"
      }
    },
    "preferences": {
      "type": "object",
      "description": "User preferences for the DatasetStructureModel",
      "required": ["defaultDataLocationIdentifier", "environmentIdentifier"],
      "additionalProperties": false,
      "properties": {
        "defaultDataLocationIdentifier": {
          "type": "string",
          "description": "Identifier of the default data location"
        },
        "environmentIdentifier": {
          "type": "string",
          "description": "Identifier of the current computing environment (e.g., 'windows-lab', 'mac-home'). Used to select the appropriate rootStoragePath when multiple environments are defined."
        }
      }
    }
  },
  "definitions": {
    "dataLocation": {
      "type": "object",
      "required": ["displayName", "identifier", "dataCategory", "sourceType"],
      "additionalProperties": false,
      "allOf": [
        {
          "if": {
            "properties": {
              "sourceType": { "const": "filesystem" }
            }
          },
          "then": {
            "required": ["filesystemSource"]
          }
        },
        {
          "if": {
            "properties": { "sourceType": { "const": "spreadsheet" } }
          },
          "then": {
            "required": ["spreadsheetSource", "entityType"]
          }
        },
        {
          "if": {
            "properties": { "sourceType": { "const": "database" } }
          },
          "then": {
            "required": ["databaseSource", "entityType"]
          }
        },
        {
          "if": {
            "properties": { "sourceType": { "const": "api" } }
          },
          "then": {
            "required": ["apiSource", "entityType"]
          }
        }
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "description": "Unique identifier for this data location, used in cross-references such as derivedFrom and preferences.defaultDataLocationIdentifier"
        },
        "displayName": {
          "type": "string",
          "description": "Human-readable name of the data location"
        },
        "description": {
          "type": "string",
          "description": "Optional detailed description of the data location's purpose"
        },
        "dataCategory": {
          "type": "string",
          "description": "Category of the data location defining its role in the data lifecycle",
          "enum": ["raw", "processed", "derived", "imported", "reference", "temporary", "archive", "custom"],
          "default": "raw"
        },
        "sourceType": {
          "type": "string",
          "enum": ["filesystem", "spreadsheet", "database", "api"],
          "description": "The type of data source this location represents. Determines which source-specific block is required: 'filesystem' requires filesystemSource; 'spreadsheet', 'database', and 'api' require their respective source block and entityType."
        },
        "entityType": {
          "type": "string",
          "description": "The entity type that this data location provides records for. Must match a name in the top-level entityTypes. For filesystem sources this is optional (can be inferred from entityLayout). For spreadsheet, database, and API sources this should be specified."
        },
        "derivedFrom": {
          "type": "array",
          "description": "Identifiers of data locations that are the source inputs for this location. Declares provenance: this location's data was produced from the listed locations. Used by tools to (1) resolve which source entities to pull metadata from when generating output paths, and (2) build a data lineage graph across locations.",
          "items": {
            "type": "string"
          }
        },
        "tags": {
          "type": "array",
          "description": "Optional free-form tags for categorization, filtering, or search",
          "items": {
            "type": "string"
          }
        },
        "customProperties": {
          "type": "object",
          "description": "Optional free-form key-value pairs for domain-specific or tool-specific metadata not covered by the schema. Values can be any JSON type."
        },
        "filesystemSource": {
          "$ref": "#/definitions/filesystemSource"
        },
        "spreadsheetSource": {
          "$ref": "#/definitions/spreadsheetSource"
        },
        "databaseSource": {
          "$ref": "#/definitions/databaseSource"
        },
        "apiSource": {
          "$ref": "#/definitions/apiSource"
        }
      }
    },
    "spreadsheetSource": {
      "type": "object",
      "description": "Configuration for reading entity records from a spreadsheet file. Each row becomes one entity record.",
      "required": ["filePath", "columnMapping"],
      "additionalProperties": false,
      "properties": {
        "filePath": {
          "type": "string",
          "description": "Path to the spreadsheet file (.xlsx, .csv, .tsv). Can be relative to the project root or absolute."
        },
        "sheetName": {
          "type": "string",
          "description": "Sheet name to read. If omitted, reads the first sheet."
        },
        "headerRow": {
          "type": "integer",
          "description": "1-based row number containing column headers.",
          "default": 1
        },
        "columnMapping": {
          "type": "array",
          "description": "Maps spreadsheet columns to global metadata definitions. Each entry links one column to one metadata field.",
          "items": {
            "$ref": "#/definitions/columnMappingEntry"
          }
        }
      }
    },
    "databaseSource": {
      "type": "object",
      "description": "Configuration for reading entity records from a database table or view. Each row becomes one entity record.",
      "required": ["connectionRef", "tableName", "columnMapping"],
      "additionalProperties": false,
      "properties": {
        "connectionRef": {
          "type": "string",
          "description": "Identifier referencing an externally stored connection configuration. The actual connection details (host, credentials) must be stored outside this schema, never inline."
        },
        "tableName": {
          "type": "string",
          "description": "Table or view name to query"
        },
        "columnMapping": {
          "type": "array",
          "description": "Maps database columns to global metadata definitions.",
          "items": {
            "$ref": "#/definitions/columnMappingEntry"
          }
        }
      }
    },
    "apiSource": {
      "type": "object",
      "description": "Configuration for reading entity records from a remote API endpoint.",
      "required": ["connectionRef", "endpoint", "fieldMapping"],
      "additionalProperties": false,
      "properties": {
        "connectionRef": {
          "type": "string",
          "description": "Identifier referencing an externally stored API configuration (base URL, authentication). Never store credentials inline."
        },
        "endpoint": {
          "type": "string",
          "description": "API endpoint path for listing entities"
        },
        "fieldMapping": {
          "type": "array",
          "description": "Maps response fields to global metadata definitions.",
          "items": {
            "type": "object",
            "required": ["field", "metadataRef"],
            "additionalProperties": false,
            "properties": {
              "field": {
                "type": "string",
                "description": "Field name or dot-separated path in the API response (e.g., 'subject.id')"
              },
              "metadataRef": {
                "type": "string",
                "description": "Key in the top-level metadataDefinitions that this field provides values for"
              }
            }
          }
        }
      }
    },
    "columnMappingEntry": {
      "type": "object",
      "description": "Maps a single column in a spreadsheet or database to a global metadata definition.",
      "required": ["column", "metadataRef"],
      "additionalProperties": false,
      "properties": {
        "column": {
          "type": "string",
          "description": "Column header name as it appears in the spreadsheet or database"
        },
        "metadataRef": {
          "type": "string",
          "description": "Key in the top-level metadataDefinitions that this column provides values for"
        }
      }
    },
    "filesystemSource": {
      "type": "object",
      "description": "Configuration for a filesystem-based data location where entities are discovered by traversing a folder/file hierarchy.",
      "required": ["rootStoragePaths", "entityLayout"],
      "additionalProperties": false,
      "properties": {
        "rootStoragePaths": {
          "type": "array",
          "description": "Array of storage root path configurations. Supports multiple environments (e.g., Windows lab, Mac analysis) and multiple storage locations for distributed datasets.",
          "items": {
            "$ref": "#/definitions/rootStoragePath"
          }
        },
        "entityLayout": {
          "type": "array",
          "description": "Ordered definition of the physical hierarchy that maps folder and file structure to semantic entities. Each item represents one level of the hierarchy, from outermost (index 0) to innermost.",
          "items": {
            "$ref": "#/definitions/entityLayoutLevel"
          }
        },
        "metadataMapping": {
          "type": "array",
          "description": "Defines how metadata fields (declared globally in metadataDefinitions) are extracted from filenames or folder names in this data location. Each item maps one global metadata field to a location-specific extraction rule. No new metadata fields should be defined here.",
          "items": {
            "type": "object",
            "required": ["metadataRef", "extraction"],
            "additionalProperties": false,
            "properties": {
              "metadataRef": {
                "type": "string",
                "description": "Key of the global metadata definition in metadataDefinitions that this extraction rule populates"
              },
              "extraction": {
                "$ref": "#/definitions/metadataExtraction",
                "description": "Location-specific extraction rules for this metadata field"
              }
            }
          }
        },
        "pathTemplate": {
          "type": "string",
          "description": "Optional human-readable template showing the full expected path structure with placeholders (e.g., '{rootPath}/{subject}/{session}'). Informational only — the authoritative path structure is defined by entityLayout.",
          "default": null
        },
        "additionalFolders": {
          "type": "array",
          "description": "List of folder names that may exist within the leaf level of this data location but are not part of the entity hierarchy (e.g., 'logs', 'temp')",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "rootStoragePath": {
      "type": "object",
      "required": ["identifier", "path"],
      "additionalProperties": false,
      "properties": {
        "identifier": {
          "type": "string",
          "description": "Unique identifier for this storage path entry"
        },
        "path": {
          "type": "string",
          "description": "Absolute file system path to the root of this data location on this environment"
        },
        "volumeName": {
          "type": "string",
          "description": "Name of the disk or volume where the path is located (useful for disambiguating removable or network drives)"
        },
        "storageType": {
          "type": "string",
          "description": "Type of storage medium",
          "enum": ["local", "external", "network", "cloud", "removable", "virtual"],
          "default": "local"
        },
        "environment": {
          "type": "string",
          "description": "Computing environment identifier this path is valid for. Must match a value that will be used in preferences.environmentIdentifier (e.g., 'windows-lab', 'mac-analysis')."
        },
        "priority": {
          "type": "integer",
          "description": "Selection priority when multiple rootStoragePaths match the current environment. Lower values are preferred. Useful when a location is mirrored across multiple drives.",
          "default": 1
        },
        "isAvailable": {
          "type": "boolean",
          "description": "Runtime hint indicating whether this path is currently accessible. Can be set by tools after checking path availability. Does not affect schema validation.",
          "default": true
        }
      }
    },
    "entityLayoutLevel": {
      "type": "object",
      "required": ["name", "entityType"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of this hierarchy level (e.g., 'subjects', 'sessions')"
        },
        "entityType": {
          "type": "string",
          "description": "The entity type this level represents. Should match the name of an entry in entityTypes (e.g., 'subject', 'session', 'recording').",
          "examples": ["subject", "session", "recording", "experiment", "run", "scan", "trial"]
        },
        "fileSystemType": {
          "type": "string",
          "description": "Whether entities at this level are represented by folders or individual files",
          "enum": ["folder", "file"],
          "default": "folder"
        },
        "pathComponentTemplate": {
          "type": "string",
          "description": "Template defining how this level's name is composed from metadata fields (e.g., '{subject_id}', 'session-{session_id}', '{subject_id}_{acquisition_date}'). Tokens in curly braces reference keys in metadataDefinitions. For derived data locations, this template is used to *generate* new folder names by substituting metadata values from the source entity. matchPattern is optional when this field is present, as tools can derive a matching regex from the template."
        },
        "matchPattern": {
          "type": "string",
          "description": "Regular expression pattern for matching valid entity names at this level (e.g., '^[A-Za-z0-9]+$'). Required for non-derived locations. Optional when pathComponentTemplate is present, in which case tools may auto-derive a regex from the template."
        },
        "excludePatterns": {
          "type": "array",
          "description": "Regex or glob patterns for folder or file names to exclude at this level (e.g., 'temp', 'backup', '^\\..*')",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "isRequired": {
          "type": "boolean",
          "description": "Whether this level must exist in the hierarchy. If false, the level is optional and its absence does not constitute an error.",
          "default": true
        },
        "isVariable": {
          "type": "boolean",
          "description": "Whether entities at this level have variable names (true, the common case) or a single fixed name (false, use fixedName).",
          "default": true
        },
        "fixedName": {
          "type": "string",
          "description": "The fixed folder name when isVariable is false (e.g., 'processed', 'raw')"
        },
        "filePatterns": {
          "type": "array",
          "description": "Patterns for grouping files at this level, if fileSystemType is 'file'. Sometimes, files of multiple entities are present within one folder, and this property is used to group such files by their respective entity.",
          "items": {
            "$ref": "#/definitions/fileGroupingPattern"
          }
        }
      }
    },
    "fileGroupingPattern": {
      "type": "object",
      "description": "Describes a grouping pattern for files associated with an entity at a given hierarchy level",
      "required": ["pattern"],
      "additionalProperties": false,
      "properties": {
        "pattern": {
          "type": "string",
          "description": "Regular expression pattern for capturing groups of files (e.g., '.*\\.tif$', '.*_metadata\\.json$')"
        },
        "isRequired": {
          "type": "boolean",
          "description": "Whether a file matching this pattern must exist for the entity to be considered complete",
          "default": false
        }
      }
    },
    "metadataExtraction": {
      "type": "object",
      "description": "Instructions for how to extract or derive a metadata value from a path component or file content",
      "required": ["method"],
      "additionalProperties": false,
      "properties": {
        "method": {
          "type": "string",
          "description": "Extraction method to apply",
          "enum": ["substring", "regex", "function", "template", "fixed", "filename", "filepath", "sidecar"]
        },
        "pattern": {
          "type": "string",
          "description": "Pattern argument for the method. For 'regex': the capture group pattern. For 'substring': the slice specification (e.g., '0:8'). For 'template': the template string with {token} placeholders."
        },
        "valueFormat": {
          "type": "string",
          "description": "Format string for parsing or formatting the extracted value (e.g., 'yyyyMMdd' for date parsing)"
        },
        "extractorFunction": {
          "type": "string",
          "description": "Name of a user-defined function to call for 'function' method extraction. The function receives the path component string and returns the metadata value."
        },
        "entityLayoutLevel": {
          "type": ["integer", "null", "string"],
          "description": "Index (0-based integer) or name (string) of the entityLayout level from which to extract this metadata. Required when a single level is the source. Null or omitted means the full path is used."
        },
        "filePattern": {
          "type": "string",
          "description": "Glob pattern to locate the sidecar file within the entity's folder (e.g., '*_metadata.json'). Required when method is 'sidecar'."
        },
        "contentPath": {
          "type": "string",
          "description": "Path to the value within the sidecar file. For JSON/YAML: dot notation (e.g., 'imaging.depth_um'). For CSV: column name. For key-value text: key name. Used when method is 'sidecar'."
        },
        "fileFormat": {
          "type": "string",
          "description": "Format of the sidecar file. Used when method is 'sidecar'.",
          "enum": ["json", "yaml", "csv", "ini", "xml", "text"]
        },
        "normalize": {
          "type": "string",
          "description": "Normalization applied after extraction to ensure consistent values across data locations. Particularly important for fields used in identifierRef cross-location matching.",
          "enum": ["lowercase", "uppercase", "trim", "strip_prefix", "strip_suffix", "none"],
          "default": "none"
        },
        "normalizePattern": {
          "type": "string",
          "description": "Argument for pattern-based normalizations. For 'strip_prefix': the prefix to remove (e.g., 'sub-'). For 'strip_suffix': the suffix to remove."
        }
      }
    },
    "entityRelationship": {
      "type": "object",
      "description": "Defines a semantic relationship between two entity types in this dataset, independent of physical storage layout",
      "required": ["sourceEntity", "targetEntity", "relationType"],
      "additionalProperties": false,
      "properties": {
        "sourceEntity": {
          "type": "string",
          "description": "Name of the source entity type (must match a name in entityTypes)"
        },
        "targetEntity": {
          "type": "string",
          "description": "Name of the target entity type (must match a name in entityTypes)"
        },
        "relationType": {
          "type": "string",
          "enum": ["oneToOne", "oneToMany", "manyToOne", "manyToMany"],
          "description": "Cardinality of the relationship from sourceEntity to targetEntity"
        },
        "relationName": {
          "type": "string",
          "description": "Optional semantic name for this relationship (e.g., 'hasSessions', 'belongsTo', 'contains')"
        },
        "isRequired": {
          "type": "boolean",
          "description": "Whether this relationship must hold for all instances. If true, every sourceEntity instance must have at least one related targetEntity instance.",
          "default": true
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of what this relationship means (e.g., 'A subject can have multiple recording sessions over time')"
        }
      }
    }
  }
}
